const backgroundName = `background-0${Math.ceil(Math.random()*3)}`
document.body.style.background = `url(./images/${backgroundName}.jpg) no-repeat 50% 50%`;
document.body.style.backgroundSize = `cover`;